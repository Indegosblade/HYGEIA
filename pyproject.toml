[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hygeia"
version = "3.1.0"
description = "Cross-platform forensic PII sanitization — 20 tested handlers for Chrome, Firefox, iOS, Android, Windows, macOS, Linux + generic SQLite/text scanning"
readme = "README.md"
license = {text = "PolyForm-Noncommercial-1.0.0"}
requires-python = ">=3.10"
authors = [
    {name = "Kevin Estrada", email = "estradakh@gmail.com"},
]
keywords = ["sanitization", "forensics", "security", "privacy", "pii", "hipaa", "gdpr", "ccpa"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Information Technology",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Security",
    "Topic :: System :: Filesystems",
    "Topic :: Scientific/Engineering :: Information Analysis",
]

[project.urls]
Repository = "https://github.com/Indegosblade/HYGEIA"
Issues = "https://github.com/Indegosblade/HYGEIA/issues"

[project.scripts]
hygeia = "hygeia.cli:main"

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "ruff>=0.4",
    "mypy>=1.8",
    "bandit>=1.7",
]

[tool.mypy]
python_version = "3.12"
warn_unused_configs = true
ignore_missing_imports = true
disallow_untyped_defs = false
check_untyped_defs = false

[tool.bandit]
exclude_dirs = ["tests"]
skips = ["B101", "B110", "B404", "B405", "B314", "B603", "B607", "B608"]

[tool.setuptools.packages.find]
include = ["hygeia*"]

[tool.setuptools.package-data]
hygeia = ["rules/*.json"]
